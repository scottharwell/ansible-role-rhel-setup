---
- hosts: localhost
  become: true

  vars:
    ssh_public_keys:
      - "{{ lookup('file', lookup('env','HOME') + '/.ssh/id_ed25519.pub') }}"
    users:
      - username: scott
        home_path: /home/scott

  roles:
    - role: "./"
